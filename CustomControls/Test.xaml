<UserControl
    x:Name="UserControl"
    x:Class="CustomControls.Test"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel>

        <StackPanel Orientation="Horizontal">

            <CheckBox IsChecked="{Binding IsChecked, ElementName=UserControl}"/>

            <Image Margin="-80 5 5 5" 
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       Source="{Binding ImagePath, ElementName=UserControl}" 
                       MaxWidth="350" />

        </StackPanel>

        <TextBlock HorizontalAlignment="Center"
                   Text="{Binding Similarity, ElementName=UserControl}"
                   Margin="5"/>
    </StackPanel>

</UserControl>